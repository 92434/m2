# ================================
# 🏰 数据要塞核心配置文件
# 来自白昼组织的数字堡垒
# ================================

fortress:
  name: "白昼要塞"
  version: "1.0.0"
  codename: "白昼一号数据壁垒"
  organization: "白昼"
  
security:
  encryption_level: "AES-256-GCM"
  authentication:
    method: "biometric-dual-factor"
    timeout: 300
  firewall:
    enabled: true
    rules:
      - allow: "internal_network"
        ports: [8080, 8443, 2222]
      - deny: "external_access"
        except: ["authorized_terminals"]
  
storage:
  primary_drive: "/dev/sda1"
  backup_system: 
    enabled: true
    locations:
      - "/backup/local"
      - "s3://baizhou-fortress-backup"
  data_shards: 7
  parity_shards: 3
  
network:
  internal_ip: "192.168.1.100"
  external_port: 8443
  vpn_enabled: true
  tunnel_protocol: "wireguard"
  
monitoring:
  heartbeat_interval: 60
  alert_threshold: 85
  log_retention: "90d"
  
modules:
  - name: "数据核心"
    status: "active"
    priority: "critical"
  - name: "防御系统"
    status: "standby"
    priority: "high"
  - name: "传输通道"
    status: "operational"
    priority: "medium"

# ================================
# 🔐 要塞密钥配置
# ================================
keys:
  master_key: "${{ secrets.FORTRESS_MASTER_KEY }}"
  backup_keys:
    - "${{ secrets.BACKUP_KEY_1 }}"
    - "${{ secrets.BACKUP_KEY_2 }}"
  session_keys:
    rotation_interval: "24h"
    algorithm: "ChaCha20-Poly1305"